*Declare a time type.

REPORT Z_ABAP101_005.

*We can declare time type using the 'TIMS' data type.

TYPES lv_time TYPE TIMS.

*Thisline declares variable named 'lv_time' of type 'TIMS'.
*which is used to store time values in ABAP.
*or

TYPES last_changed_at TYPE t.

*We can use these variables to handle time-related information within our ABAP programs.
