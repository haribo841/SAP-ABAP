*Declare a time type.

REPORT Z_ABAP101_005.

*We can declare a time type using the 'TIMS' data type.

DATA lv_time TYPE TIMS.

*This line declares variable named 'lv_type' of type 'TIMS',
*which is used to store time values in ABAP.
*or

TYPES last_changed_at TYPE t.

*We can use these variable to handle time-related information within our ABAP programs.